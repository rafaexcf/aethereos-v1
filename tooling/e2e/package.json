{
  "name": "@aethereos/e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright E2E tests — real browser SCP pipeline validation",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:scp-pipeline": "playwright test tests/scp-pipeline.spec.ts",
    "test:headed": "playwright test --headed",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "typescript": "^5.4.0"
  }
}
